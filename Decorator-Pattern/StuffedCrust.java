public class StuffedCrust implements PizzaBase { // This creates the public class "StuffedCrust" and implements it to the interface "PizzaBase"

    @Override
    public void selection() { // This is a public void function that returns nothing
        System.out.printIn("StuffedCrust Selected");  // This tells the system to print out "StuffedCrust Selected"
    }
}
